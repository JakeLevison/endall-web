#!/bin/sh

# Banned marketing copy — pricing, pilot offers, contract terms, positioning
# violations, and restricted marketing phrases. No pricing, pilot language, or
# contract terms anywhere in the codebase until management locks pricing.
# See CLAUDE.md "UI Copy Hard Rules" for full rationale.

BANNED='\$199|199/month|free pilot|free trial|60-day free|60 day free|60-day pilot|60 day pilot|60-day onboarding|60 day onboarding|No credit card|no contract|No contract|Start your pilot|data migration|Live in under a week'

if grep -rE "$BANNED" src/ --include="*.tsx" --include="*.ts" --include="*.jsx" --include="*.js"; then
  echo ""
  echo "❌ Banned marketing copy found in src/. Remove before committing."
  echo "   See CLAUDE.md → UI Copy Hard Rules for the full list."
  exit 1
fi
